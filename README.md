# Aoun — Next.js App

Small, opinionated Next.js starter used in this workspace.

## Overview

A clean Next.js + TypeScript project with Tailwind and a small component library under `components/` and `components/ui/`.

## Quick Start

Prerequisites:
- **Node**: 18+ (recommended)
- **pnpm**: recommended package manager

Install dependencies:

```bash
pnpm install
```

Run in development mode:

```bash
pnpm dev
```

Build for production:

```bash
pnpm build
```

Run the production build locally:

```bash
pnpm start
```

Lint the project:

```bash
pnpm lint
```

These scripts are defined in `package.json`.

## Project Structure (important paths)

- `app/` — Next.js App Router pages and layout
- `components/` — App screens and shared components
- `components/ui/` — Re-usable UI primitives and atoms
- `hooks/` — Custom React hooks
- `lib/` — Small utilities and helpers
- `public/` — Static assets
- `styles/` — Global CSS and Tailwind setup

## Environment

If your app requires secrets or runtime configuration, add an `.env.local` at the project root.

Example:

```
NEXT_PUBLIC_API_URL=https://api.example.com
```

## Deployment

This app is compatible with Vercel and most Node-capable hosts. Typical flow:

- Build: `pnpm build`
- Start server: `pnpm start`

For Vercel, connect the repository and set `pnpm install` as the install step (Vercel auto-detects Next.js projects).

## Contributing & Next Steps

- Add a `LICENSE` file if you want an explicit license.
- Add CI (GitHub Actions) to run `pnpm build` and `pnpm lint` on PRs.
- Document any required environment variables in this README or `.env.example`.

If you'd like, I can:
- open a PR with a `LICENSE` and `.env.example`
- add a simple GitHub Actions workflow for build + lint

---
Generated by your assistant. See `package.json` for the exact scripts.
